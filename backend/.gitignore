node_modules
dist
# User uploads (images/PDFs) stay local — do not commit them. The folder itself is kept via uploads/.gitkeep.
uploads/*
!uploads/.gitkeep
.env
npm-debug.log*
yarn-debug.log*
yarn-error.log*

