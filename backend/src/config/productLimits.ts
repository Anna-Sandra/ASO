/** Max image URLs stored on one product (large galleries; still bounded for DB size). */
export const MAX_PRODUCT_GALLERY_IMAGES = 500;

/** Max files accepted in one multipart upload request (batched on the client for larger sets). */
export const MAX_PRODUCT_IMAGES_PER_UPLOAD = 40;
