/** Official marketplace name (all caps). Use for UI fallbacks when platform settings are not loaded. */
export const SITE_NAME = "SHOPIQGH";

/** Admin / support inbox label shown in buyer and vendor messaging. */
export const SUPPORT_LABEL = "SHOPIQGH Support";

/** Short tagline for meta and marketing. */
export const SITE_TAGLINE = "Ghana's general marketplace — food, fashion, electronics, services, and more.";
